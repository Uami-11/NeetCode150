# Two Integer Sum II

This is like two sum but sorted in ascending order (non decreasing). We can take advantage of the fact that its ascending so we know if either end of a two pointer is pulling their weight in finding the target

