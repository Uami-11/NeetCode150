# Valid Sudoku

## The Solution

The solution is quite simple. You just have to go to each row and each column and each square and check for duplicates the same way. It takes O(n^2) time and O(n) space. There are other methods to do this, like with hash sets but it looks like its n squared space, so idk why you would do that
